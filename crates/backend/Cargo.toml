[package]
name = "{{crate_name}}_backend"
version.workspace = true
edition.workspace = true
authors.workspace = true
description.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
neon = { version = "1", default-features = false, features = ["napi-6"] }
